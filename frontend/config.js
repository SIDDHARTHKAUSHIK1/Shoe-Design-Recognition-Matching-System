/**
 * ShoeMatch AI — Deployment Configuration
 * ---------------------------------------
 * Set this to the public URL of your FastAPI backend, with NO trailing slash.
 *
 *   Same-server deploy (frontend + API together):  leave as ""
 *   Split deploy (static UI on Hostinger, API elsewhere):
 *       window.SHOEMATCH_API_BASE = "https://api.yourdomain.com";
 *
 * This is the ONLY file that needs editing when the backend moves hosts.
 * A value saved in localStorage (Settings screen) still overrides this.
 */
window.SHOEMATCH_API_BASE = "";

